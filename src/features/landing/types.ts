export interface PortalCardItem {
  id: string;
  numberRoman: string;
  title: string;
  description: string;
  route: string;
  ctaText: string;
}

export interface ScreeningPillar {
  title: string;
  description: string;
}
